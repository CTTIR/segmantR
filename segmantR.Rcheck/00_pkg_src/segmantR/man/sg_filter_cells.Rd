% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask_ops.R
\name{sg_filter_cells}
\alias{sg_filter_cells}
\title{Filter cells by morphological criteria}
\usage{
sg_filter_cells(
  mask,
  min_area = 50L,
  max_area = 5000L,
  min_circularity = 0.3,
  max_eccentricity = 0.95,
  border_cells = c("keep", "remove", "flag")
)
}
\arguments{
\item{mask}{An \code{sg_mask} object.}

\item{min_area}{Integer. Minimum cell area in pixels. Default \code{50L}.}

\item{max_area}{Integer. Maximum cell area in pixels. Default \code{5000L}.}

\item{min_circularity}{Numeric in [0, 1]. Minimum circularity
(4 * pi * area / perimeter^2). Default \code{0.3}.}

\item{max_eccentricity}{Numeric in [0, 1]. Maximum eccentricity.
Default \code{0.95}.}

\item{border_cells}{Character. How to handle cells touching the image
border: \code{"keep"} (default), \code{"remove"}, or \code{"flag"}.}
}
\value{
A filtered \code{sg_mask} object. When \code{border_cells = "flag"}, the
returned mask has an additional \code{border_cell_ids} element.
}
\description{
Removes cells from a segmentation mask that fall outside specified
morphological thresholds. Useful for quality control after initial
segmentation.
}
\examples{
labels <- matrix(0L, nrow = 20, ncol = 20)
labels[3:8, 3:8] <- 1L
labels[12:18, 12:18] <- 2L
mask <- new_sg_mask(labels)
filtered <- sg_filter_cells(mask, min_area = 10L, max_area = 100L)
print(filtered)
}
