% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{sg_apply_corrections}
\alias{sg_apply_corrections}
\title{Apply manual corrections to a segmentation mask}
\usage{
sg_apply_corrections(mask, corrections)
}
\arguments{
\item{mask}{An \code{sg_mask} object.}

\item{corrections}{A list of correction operations. Each element is a list
with at least an \verb{$action} field (\code{"split"}, \code{"merge"}, \code{"delete"}, or
\code{"add"}). See Details.}
}
\value{
A corrected \code{sg_mask} object.
}
\description{
Takes a list of correction operations (split, merge, delete, add) and
applies them to produce a corrected mask.
}
\details{
Each correction in the list must contain:
\describe{
\item{\code{action = "delete"}}{Remove cell. Requires \verb{$cell_id}.}
\item{\code{action = "merge"}}{Merge cells. Requires \verb{$cell_ids} (integer vector).}
\item{\code{action = "split"}}{Split a cell. Requires \verb{$cell_id} and
\verb{$seed_points} (matrix with columns row, col).}
\item{\code{action = "add"}}{Add a new cell. Requires \verb{$pixels} (matrix of
row/col coordinates).}
}
}
\examples{
labels <- matrix(0L, nrow = 20, ncol = 20)
labels[3:8, 3:8] <- 1L
labels[12:18, 12:18] <- 2L
mask <- new_sg_mask(labels)
corrected <- sg_apply_corrections(mask, list(
  list(action = "delete", cell_id = 2L)
))
print(corrected)
}
