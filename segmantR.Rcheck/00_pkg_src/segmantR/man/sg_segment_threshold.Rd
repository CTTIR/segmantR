% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_threshold.R
\name{sg_segment_threshold}
\alias{sg_segment_threshold}
\title{Threshold-based cell segmentation}
\usage{
sg_segment_threshold(
  image,
  channel = 1L,
  method = c("otsu", "adaptive", "triangle"),
  block_size = 51L,
  offset = 0.05,
  morphology = list(open = 5L, fill_holes = TRUE),
  min_area = 50L,
  max_area = 5000L
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{channel}{Integer index of the image channel to threshold.
Default is \code{1L}.}

\item{method}{Character string specifying the thresholding method.
One of \code{"otsu"} (default), \code{"adaptive"}, or \code{"triangle"}.}

\item{block_size}{Integer block size for adaptive thresholding.
Must be a positive odd integer. Only used when
\code{method = "adaptive"}. Default is \code{51L}.}

\item{offset}{Numeric offset subtracted from the local mean in
adaptive thresholding. Default is \code{0.05}.}

\item{morphology}{A named list controlling morphological cleanup:
\describe{
\item{\code{open}}{Integer; structuring element size for opening
(erosion then dilation). Set to \code{0L} to skip. Default \code{5L}.}
\item{\code{fill_holes}}{Logical; whether to fill holes inside
objects. Default \code{TRUE}.}
}}

\item{min_area}{Integer; minimum object area in pixels. Objects
smaller than this are removed. Default is \code{50L}.}

\item{max_area}{Integer; maximum object area in pixels. Objects
larger than this are removed. Default is \code{5000L}.}
}
\value{
An \code{sg_mask} object with labelled cell regions.
}
\description{
Segments cells in an \code{sg_image} by applying a global or local
intensity threshold, followed by morphological cleanup and
connected-component labelling with size filtering.
}
\examples{
set.seed(42)
pixels <- matrix(runif(400), nrow = 20, ncol = 20)
img <- new_sg_image(pixels)
mask <- sg_segment_threshold(img, method = "otsu")
print(mask)
}
