% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/training.R
\name{sg_train_cellpose}
\alias{sg_train_cellpose}
\title{Fine-tune a Cellpose model on user-corrected masks}
\usage{
sg_train_cellpose(
  training_data,
  base_model = "cyto3",
  n_epochs = 100L,
  learning_rate = 0.1,
  save_path = NULL,
  verbose = TRUE
)
}
\arguments{
\item{training_data}{A list of training pairs. Each element must be a list
with \verb{$image} (\code{sg_image}) and \verb{$mask} (\code{sg_mask}).}

\item{base_model}{Character. Cellpose base model to fine-tune.
Default \code{"cyto3"}.}

\item{n_epochs}{Integer. Number of training epochs. Default \code{100L}.}

\item{learning_rate}{Numeric. Learning rate. Default \code{0.1}.}

\item{save_path}{Character or \code{NULL}. Directory to save the trained model.
When \code{NULL}, a temporary directory is used.}

\item{verbose}{Logical. Print training progress? Default \code{TRUE}.}
}
\value{
An \code{sg_trained_model} object.
}
\description{
Trains (fine-tunes) a Cellpose segmentation model using corrected
image-mask pairs provided by the user. Requires a working Python
environment with Cellpose installed (see \code{\link[=sg_setup_python]{sg_setup_python()}}).
}
\examples{
\donttest{
# Requires Python + Cellpose:
# trained <- sg_train_cellpose(training_data, base_model = "cyto3")
}
}
