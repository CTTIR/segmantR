% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask_ops.R
\name{sg_mask_to_polygons}
\alias{sg_mask_to_polygons}
\title{Convert mask to polygon geometries}
\usage{
sg_mask_to_polygons(mask, simplify = TRUE, tolerance = 1)
}
\arguments{
\item{mask}{An \code{sg_mask} object.}

\item{simplify}{Logical. Simplify polygon geometries? Default \code{TRUE}.}

\item{tolerance}{Numeric. Simplification tolerance in pixels when
\code{simplify = TRUE}. Default \code{1.0}.}
}
\value{
An \code{sf} data frame with one row per cell (columns \code{cell_id} and
\code{geometry}) when \pkg{sf} is available, or a tibble with columns
\code{cell_id}, \code{row}, \code{col} listing boundary coordinates.
}
\description{
Extracts cell boundaries from a label mask and returns them as polygon
geometries. If the \pkg{sf} package is available, returns an \code{sf} data
frame; otherwise returns a tibble of boundary coordinates.
}
\examples{
labels <- matrix(0L, nrow = 20, ncol = 20)
labels[3:8, 3:8] <- 1L
labels[12:18, 12:18] <- 2L
mask <- new_sg_mask(labels)
polys <- sg_mask_to_polygons(mask, simplify = FALSE)
head(polys)
}
