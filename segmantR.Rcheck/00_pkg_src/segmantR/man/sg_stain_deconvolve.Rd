% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{sg_stain_deconvolve}
\alias{sg_stain_deconvolve}
\title{Separate H&E stain channels by colour deconvolution}
\usage{
sg_stain_deconvolve(image, method = c("macenko", "ruifrok"), stains = NULL)
}
\arguments{
\item{image}{An \code{sg_image} object with at least 3 colour channels
(RGB).}

\item{method}{Character string specifying the deconvolution method.
One of \code{"macenko"} (default) or \code{"ruifrok"}.}

\item{stains}{Optional 3x3 numeric matrix whose columns are the
stain vectors. If \code{NULL}, stain vectors are estimated from the
image using the specified \code{method}.}
}
\value{
A named list of \code{sg_image} objects, one per stain channel.
Typically \code{hematoxylin} and \code{eosin}, plus \code{residual}.
}
\description{
Applies colour deconvolution to an H&E-stained image to separate
the haematoxylin and eosin channels.
}
\examples{
\donttest{
# Requires a colour image
pixels <- array(runif(300), dim = c(10, 10, 3))
img <- new_sg_image(pixels, channels = c("R", "G", "B"))
stains <- sg_stain_deconvolve(img)
names(stains)
}
}
