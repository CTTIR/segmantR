% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/preprocess.R
\name{sg_preprocess}
\alias{sg_preprocess}
\title{Preprocess an image}
\usage{
sg_preprocess(
  image,
  stain_normalize = c("none", "macenko", "reinhard", "vahadane"),
  denoise = FALSE,
  contrast = c("none", "clahe", "stretch"),
  target_resolution = NULL
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{stain_normalize}{Character string specifying the stain
normalisation method. One of \code{"none"} (default), \code{"macenko"},
\code{"reinhard"}, or \code{"vahadane"}.}

\item{denoise}{Logical; if \code{TRUE}, a simple mean filter is applied
for denoising. Default is \code{FALSE}.}

\item{contrast}{Character string specifying contrast enhancement.
One of \code{"none"} (default), \code{"clahe"}, or \code{"stretch"}.}

\item{target_resolution}{Numeric scalar or \code{NULL}. If not \code{NULL},
the image is resampled to this target resolution (microns per
pixel) using bilinear interpolation.}
}
\value{
A new \code{sg_image} object with its \code{history} field updated to
record the preprocessing steps applied.
}
\description{
Applies a chain of preprocessing steps to an \code{sg_image} object,
including optional stain normalisation, denoising, and contrast
enhancement.
}
\examples{
pixels <- matrix(runif(400), nrow = 20, ncol = 20)
img <- new_sg_image(pixels)
img2 <- sg_preprocess(img)
img3 <- sg_preprocess(img, contrast = "stretch", denoise = TRUE)
}
