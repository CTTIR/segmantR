% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_watershed.R
\name{sg_segment_propagate}
\alias{sg_segment_propagate}
\title{Voronoi propagation from nuclear seeds}
\usage{
sg_segment_propagate(
  image,
  nuclear_mask,
  membrane_image = NULL,
  lambda = 0.01,
  expand_max = 20L
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{nuclear_mask}{An \code{sg_mask} object providing the nuclear seed
labels.}

\item{membrane_image}{An \code{sg_image} object (single channel) used
as a cost surface to penalise crossing membranes. If \code{NULL},
propagation proceeds uniformly.}

\item{lambda}{Numeric weight for the membrane penalty in the cost
function. Higher values make propagation stop more readily at
membranes. Default is \code{0.01}.}

\item{expand_max}{Integer; maximum number of propagation
iterations. Default is \code{20L}.}
}
\value{
An \code{sg_mask} object with expanded cell labels.
}
\description{
Expands nuclear seed regions outward using Voronoi-style
propagation, optionally constrained by a membrane signal.
When available, \code{EBImage::propagate()} is used for higher
performance; otherwise a pure-R fallback is used.
}
\examples{
set.seed(42)
pixels <- matrix(runif(400), nrow = 20, ncol = 20)
img <- new_sg_image(pixels)
seeds <- matrix(0L, nrow = 20, ncol = 20)
seeds[5, 5] <- 1L
seeds[15, 15] <- 2L
nuc_mask <- new_sg_mask(seeds)
result <- sg_segment_propagate(img, nuclear_mask = nuc_mask)
print(result)
}
