% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/evaluation.R
\name{sg_evaluate_segmentation}
\alias{sg_evaluate_segmentation}
\title{Evaluate segmentation quality}
\usage{
sg_evaluate_segmentation(
  predicted,
  ground_truth,
  metrics = c("dice", "jaccard", "aji", "panoptic_quality", "ap50", "ap75",
    "f1_detection")
)
}
\arguments{
\item{predicted}{An \code{sg_mask} object with predicted segmentation.}

\item{ground_truth}{An \code{sg_mask} object with ground truth segmentation.}

\item{metrics}{Character vector of metrics to compute. One or more of
\code{"dice"}, \code{"jaccard"}, \code{"aji"}, \code{"panoptic_quality"}, \code{"ap50"},
\code{"ap75"}, \code{"f1_detection"}. Default is all.}
}
\value{
An \code{sg_eval} S3 object (a tibble with columns \code{metric} and \code{value})
with a custom print method.
}
\description{
Computes standard instance segmentation metrics by comparing a predicted
mask against a ground truth mask.
}
\examples{
pred_labels <- matrix(0L, nrow = 20, ncol = 20)
pred_labels[3:8, 3:8] <- 1L
pred_labels[12:18, 12:18] <- 2L
gt_labels <- matrix(0L, nrow = 20, ncol = 20)
gt_labels[3:9, 3:9] <- 1L
gt_labels[12:17, 12:17] <- 2L
pred <- new_sg_mask(pred_labels)
gt <- new_sg_mask(gt_labels)
result <- sg_evaluate_segmentation(pred, gt)
print(result)
}
