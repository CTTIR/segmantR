% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{sg_package_model}
\alias{sg_package_model}
\title{Package a trained model for sharing}
\usage{
sg_package_model(
  trained_model,
  output_path,
  name = NULL,
  description = NULL,
  include_training_data = FALSE,
  format = c("segmantR", "cellpose", "both")
)
}
\arguments{
\item{trained_model}{An \code{sg_trained_model} object.}

\item{output_path}{Character. Path for the output archive file.}

\item{name}{Character or \code{NULL}. Human-readable model name.}

\item{description}{Character or \code{NULL}. Short description of the model.}

\item{include_training_data}{Logical. Include training data in the archive?
Default \code{FALSE}.}

\item{format}{Character. Archive format: \code{"segmantR"} (default),
\code{"cellpose"}, or \code{"both"}.}
}
\value{
The output file path, returned invisibly.
}
\description{
Creates a \code{.segmantR} archive (ZIP format) containing model weights,
a \code{model_card.json}, and optionally training data.
}
\examples{
\donttest{
mdl <- new_sg_trained_model(
  model_path = tempdir(),
  backend = "cellpose",
  base_model = "cyto3",
  training_metrics = list(n_epochs = 50L)
)
out <- sg_package_model(mdl, tempfile(fileext = ".segmantR"))
}
}
