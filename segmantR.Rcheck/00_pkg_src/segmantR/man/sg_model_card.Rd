% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{sg_model_card}
\alias{sg_model_card}
\title{Display a formatted model card}
\usage{
sg_model_card(trained_model)
}
\arguments{
\item{trained_model}{An \code{sg_trained_model} object.}
}
\value{
A tibble with columns \code{field} and \code{value} representing the model
card contents, returned invisibly.
}
\description{
Prints a human-readable model card for a trained segmentation model and
returns the card contents as a tibble.
}
\examples{
mdl <- new_sg_trained_model(
  model_path = tempdir(),
  backend = "cellpose",
  base_model = "cyto3",
  training_metrics = list(n_epochs = 100L),
  model_card = list(author = "Test User", tissue = "lung")
)
sg_model_card(mdl)
}
