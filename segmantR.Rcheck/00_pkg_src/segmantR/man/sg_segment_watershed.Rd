% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_watershed.R
\name{sg_segment_watershed}
\alias{sg_segment_watershed}
\title{Marker-controlled watershed segmentation}
\usage{
sg_segment_watershed(
  image,
  channel = 1L,
  seed_method = c("h_minima", "distance", "markers"),
  h = 0.05,
  min_distance = 10L,
  expand_method = c("voronoi", "dilation"),
  expand_pixels = 3L,
  membrane_channel = NULL
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{channel}{Integer index of the image channel used for
segmentation. Default is \code{1L}.}

\item{seed_method}{Character string specifying how seeds are
generated. One of \code{"h_minima"} (default), \code{"distance"}, or
\code{"markers"}.}

\item{h}{Numeric; height parameter for h-minima or distance
threshold fraction for seed detection. Default is \code{0.05}.}

\item{min_distance}{Integer; minimum pixel distance between seeds.
Default is \code{10L}.}

\item{expand_method}{Character string specifying how seed regions
are expanded. One of \code{"voronoi"} (default) or \code{"dilation"}.}

\item{expand_pixels}{Integer; number of pixels to expand beyond
the initial watershed regions. Default is \code{3L}.}

\item{membrane_channel}{Integer or \code{NULL}. If provided, this
channel is used as the topographic surface for watershed
flooding. If \code{NULL}, the inverted \code{channel} image is used.}
}
\value{
An \code{sg_mask} object with labelled cell regions.
}
\description{
Segments cells using a marker-controlled watershed algorithm.
Seeds (markers) are detected automatically via distance-transform
peaks or h-minima, and the watershed flood fills from these seeds.
}
\examples{
set.seed(42)
pixels <- matrix(runif(400), nrow = 20, ncol = 20)
img <- new_sg_image(pixels)
mask <- sg_segment_watershed(img, seed_method = "distance", h = 0.3)
print(mask)
}
