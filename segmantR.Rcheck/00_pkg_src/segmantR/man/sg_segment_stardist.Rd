% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_stardist.R
\name{sg_segment_stardist}
\alias{sg_segment_stardist}
\title{StarDist cell segmentation}
\usage{
sg_segment_stardist(
  image,
  model = c("2D_versatile_fluo", "2D_versatile_he", "2D_paper_dsb2018", "custom"),
  channel = 1L,
  prob_thresh = 0.5,
  nms_thresh = 0.4,
  scale = NULL,
  custom_model_path = NULL,
  n_tiles = NULL
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{model}{Character string specifying the StarDist model.
One of \code{"2D_versatile_fluo"} (default),
\code{"2D_versatile_he"}, \code{"2D_paper_dsb2018"}, or \code{"custom"}.}

\item{channel}{Integer index of the image channel to segment.
Default is \code{1L}.}

\item{prob_thresh}{Numeric; object probability threshold.
Objects with probability below this value are discarded.
Default is \code{0.5}.}

\item{nms_thresh}{Numeric; non-maximum suppression overlap
threshold. Overlapping detections above this threshold are
merged. Default is \code{0.4}.}

\item{scale}{Numeric vector of length 1 or 2, or \code{NULL}.
Scaling factor applied to the image before prediction.
If \code{NULL}, no scaling is applied.}

\item{custom_model_path}{Character string or \code{NULL}. Path to a
custom-trained StarDist model directory. Required when
\code{model = "custom"}.}

\item{n_tiles}{Integer vector of length 2 or \code{NULL}. Number of
tiles in each dimension for processing large images.
If \code{NULL}, StarDist determines tiling automatically.}
}
\value{
An \code{sg_mask} object with labelled cell regions.
}
\description{
Segments cells (typically nuclei) using the StarDist deep learning
model via \code{reticulate}. Requires a working Python installation
with \code{stardist} and \code{tensorflow} installed. Use
\code{\link[=sg_setup_python]{sg_setup_python()}} to configure the environment.
}
\examples{
\donttest{
pixels <- matrix(runif(400), nrow = 20, ncol = 20)
img <- new_sg_image(pixels)
# Requires Python + stardist:
# mask <- sg_segment_stardist(img, model = "2D_versatile_fluo")
}
}
