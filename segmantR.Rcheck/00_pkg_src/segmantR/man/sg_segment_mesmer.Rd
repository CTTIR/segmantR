% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_mesmer.R
\name{sg_segment_mesmer}
\alias{sg_segment_mesmer}
\title{Mesmer (DeepCell) cell segmentation}
\usage{
sg_segment_mesmer(
  image,
  compartment = c("whole-cell", "nuclear", "both"),
  image_mpp = NULL
)
}
\arguments{
\item{image}{An \code{sg_image} object. Must have at least two
channels: a nuclear channel (first) and a membrane/cytoplasm
channel (second).}

\item{compartment}{Character string specifying which compartment
to segment. One of \code{"whole-cell"} (default), \code{"nuclear"}, or
\code{"both"}.}

\item{image_mpp}{Numeric scalar or \code{NULL}. Microns per pixel of
the input image. If \code{NULL}, the value is taken from the image
resolution metadata; if that is also unavailable, \code{0.5} is
used as a default.}
}
\value{
An \code{sg_mask} object (for \code{"whole-cell"} or \code{"nuclear"})
or a named list of two \code{sg_mask} objects (for \code{"both"}).
}
\description{
Segments cells using the Mesmer deep learning model from the
DeepCell library via \code{reticulate}. Mesmer is designed for
multiplexed tissue imaging and can segment both nuclear and
whole-cell compartments.
}
\details{
Requires a working Python installation with \code{deepcell} installed.
}
\examples{
\donttest{
pixels <- array(runif(200), dim = c(10, 10, 2))
img <- new_sg_image(pixels, channels = c("nuclear", "membrane"))
# Requires Python + deepcell:
# mask <- sg_segment_mesmer(img, compartment = "whole-cell")
}
}
