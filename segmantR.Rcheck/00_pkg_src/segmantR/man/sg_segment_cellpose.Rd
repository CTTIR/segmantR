% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/segment_cellpose.R
\name{sg_segment_cellpose}
\alias{sg_segment_cellpose}
\title{Cellpose cell segmentation}
\usage{
sg_segment_cellpose(
  image,
  model = c("cyto3", "cyto2", "nuclei", "tissuenet", "livecell", "custom"),
  channels = list(cytoplasm = 0L, nucleus = 1L),
  diameter = NULL,
  flow_threshold = 0.4,
  cellprob_threshold = 0,
  custom_model_path = NULL,
  batch_size = 8L,
  tile = TRUE
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{model}{Character string specifying the Cellpose model.
One of \code{"cyto3"} (default), \code{"cyto2"}, \code{"nuclei"},
\code{"tissuenet"}, \code{"livecell"}, or \code{"custom"}.}

\item{channels}{A named list with elements \code{cytoplasm} and
\code{nucleus}, each an integer channel index (0-based, following
Cellpose convention). Default is
\code{list(cytoplasm = 0L, nucleus = 1L)}.}

\item{diameter}{Numeric or \code{NULL}. Estimated cell diameter in
pixels. If \code{NULL}, Cellpose estimates the diameter
automatically.}

\item{flow_threshold}{Numeric; flow error threshold for mask
filtering. Default is \code{0.4}.}

\item{cellprob_threshold}{Numeric; cell probability threshold.
Default is \code{0.0}.}

\item{custom_model_path}{Character string or \code{NULL}. Path to a
custom-trained Cellpose model. Required when
\code{model = "custom"}.}

\item{batch_size}{Integer; number of images to process in
parallel on the GPU. Default is \code{8L}.}

\item{tile}{Logical; if \code{TRUE} (default), large images are
processed in tiles to reduce memory usage.}
}
\value{
An \code{sg_mask} object with labelled cell regions.
}
\description{
Segments cells using the Cellpose deep learning model via
\code{reticulate}. Requires a working Python installation with
\code{cellpose} installed. Use \code{\link[=sg_setup_python]{sg_setup_python()}} to configure the
environment.
}
\examples{
\donttest{
pixels <- array(runif(300), dim = c(10, 10, 3))
img <- new_sg_image(pixels, channels = c("R", "G", "B"))
# Requires Python + cellpose:
# mask <- sg_segment_cellpose(img, model = "cyto3")
}
}
