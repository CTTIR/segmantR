% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/features.R
\name{sg_extract_features}
\alias{sg_extract_features}
\title{Extract per-cell features from an image and mask}
\usage{
sg_extract_features(
  image,
  mask,
  features = c("intensity", "morphology", "texture", "location"),
  channels = NULL
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{mask}{An \code{sg_mask} object with labels matching the image dimensions.}

\item{features}{Character vector of feature groups to compute. One or more
of \code{"intensity"}, \code{"morphology"}, \code{"texture"}, \code{"location"}. Default is
all four.}

\item{channels}{Integer or character vector selecting which image channels
to use for intensity and texture features. \code{NULL} (default) uses all
channels.}
}
\value{
A tibble with one row per cell and a \code{cell_id} column, plus columns
for each requested feature.
}
\description{
Computes intensity, morphology, texture, and location features for each
segmented cell. Returns a tibble with one row per cell.
}
\examples{
pixels <- array(runif(20 * 20 * 2), dim = c(20, 20, 2))
img <- new_sg_image(pixels, channels = c("DAPI", "CD3"))
labels <- matrix(0L, nrow = 20, ncol = 20)
labels[3:8, 3:8] <- 1L
labels[12:18, 12:18] <- 2L
mask <- new_sg_mask(labels)
feats <- sg_extract_features(img, mask, features = c("intensity", "morphology"))
print(feats)
}
