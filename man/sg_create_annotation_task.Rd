% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/annotation.R
\name{sg_create_annotation_task}
\alias{sg_create_annotation_task}
\title{Create an annotation task for human review}
\usage{
sg_create_annotation_task(
  image,
  initial_mask = NULL,
  region = NULL,
  n_patches = 20L,
  patch_size = 256L
)
}
\arguments{
\item{image}{An \code{sg_image} object.}

\item{initial_mask}{Optional \code{sg_mask} object. When provided, patches are
scored by segmentation uncertainty and sampled accordingly.}

\item{region}{Optional numeric vector \code{c(row_min, row_max, col_min, col_max)}
to restrict patch sampling to a sub-region.}

\item{n_patches}{Integer. Number of patches to sample. Default \code{20L}.}

\item{patch_size}{Integer. Size of each square patch in pixels.
Default \code{256L}.}
}
\value{
An \code{sg_annotation_task} S3 object (a list with elements \code{patches},
\code{image_id}, \code{n_patches}, \code{patch_size}, and \code{created}).
}
\description{
Samples informative image patches for manual annotation or correction.
Patches are selected on a grid or, when an initial mask is provided,
prioritised by segmentation uncertainty.
}
\examples{
pixels <- matrix(runif(100 * 100), nrow = 100, ncol = 100)
img <- new_sg_image(pixels)
task <- sg_create_annotation_task(img, n_patches = 4L, patch_size = 32L)
print(task)
}
