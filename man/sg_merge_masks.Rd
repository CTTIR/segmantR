% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mask_ops.R
\name{sg_merge_masks}
\alias{sg_merge_masks}
\title{Merge nuclear and cell body masks}
\usage{
sg_merge_masks(nuclear_mask, cell_mask, method = c("assign", "expand"))
}
\arguments{
\item{nuclear_mask}{An \code{sg_mask} object for nuclei.}

\item{cell_mask}{An \code{sg_mask} object for cell bodies.}

\item{method}{Character. \code{"assign"} (default) assigns nuclei to overlapping
cells; \code{"expand"} expands nuclear labels outward to fill cell boundaries.}
}
\value{
A list with elements \verb{$nuclear} and \verb{$cell}, both \code{sg_mask} objects
with matched cell IDs.
}
\description{
Combines a nuclear segmentation mask with a cell body segmentation mask
to produce paired nuclear-cell assignments.
}
\examples{
nuc_labels <- matrix(0L, nrow = 20, ncol = 20)
nuc_labels[5:7, 5:7] <- 1L
nuc_labels[14:16, 14:16] <- 2L
cell_labels <- matrix(0L, nrow = 20, ncol = 20)
cell_labels[3:9, 3:9] <- 1L
cell_labels[12:18, 12:18] <- 2L
nuc_mask <- new_sg_mask(nuc_labels)
cell_mask <- new_sg_mask(cell_labels)
merged <- sg_merge_masks(nuc_mask, cell_mask)
print(merged$nuclear)
}
