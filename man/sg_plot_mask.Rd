% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot.R
\name{sg_plot_mask}
\alias{sg_plot_mask}
\title{Render a mask as a coloured image}
\usage{
sg_plot_mask(
  mask,
  color_by = c("id", "area", "circularity", "cluster"),
  features = NULL,
  palette = "viridis"
)
}
\arguments{
\item{mask}{An \code{sg_mask} object.}

\item{color_by}{Character, one of \code{"id"}, \code{"area"}, \code{"circularity"},
or \code{"cluster"}.}

\item{features}{A data frame of cell features (required when \code{color_by}
is \code{"area"}, \code{"circularity"}, or \code{"cluster"}). Must contain a
\code{cell_id} column.}

\item{palette}{Character, name of a viridis palette
(default \code{"viridis"}).}
}
\value{
A \code{ggplot2} object.
}
\description{
Assigns colours to cell regions based on cell ID, area, circularity,
or cluster membership.
}
\examples{
labels <- matrix(c(0L, 1L, 1L, 2L, 2L, 0L, 3L, 3L, 0L), 3, 3)
mask <- new_sg_mask(labels)
p <- sg_plot_mask(mask)
p
}
